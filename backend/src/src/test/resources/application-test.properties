spring.datasource.url=${TEST_DATABASE_URL:jdbc:h2:mem:staff_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE}
spring.datasource.username=${TEST_DATABASE_USER:sa}
spring.datasource.password=${TEST_DATABASE_PASSWORD:}
app.demo.enabled=false
app.resend.interval-seconds=60
app.resend.max-count=3
logging.level.root=WARN

